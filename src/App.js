import { AppProvider } from './providers/AppProvider';

function App() {
    return (
        <AppProvider>
            {/* Your app components */}
        </AppProvider>
    );
}

export default App;
