
import { Request, Response, NextFunction } from "express";
import { CustomAPIError } from "../utils/customErrors";
import { logger } from "../utils/logger";

class Middlewares {
    // Setuo actual middlewares
}

export default Middlewares;
